# Copy to .env (never committed). Only HF_TOKEN is needed for training; push_env.sh sends just that to the VM.
HF_TOKEN=
# (legacy name, also accepted)
HUGGINGFACE_TOKEN=
ELEVENLABS_API_KEY=
AUDIO_DATASET_LOCATION=
OPEN_ROUTER=
